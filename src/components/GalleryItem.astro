---
import { Image } from "astro:assets";
const {image, caption, vertical = "", group} = Astro.props;
---
<a class="gallery-item" href={image.src} title={caption} data-gallery={`gallery${group}`}>
    {
        () => {
        if (vertical === "true") {
           return <Image src={image} width={250} alt={caption} />
        } else {
           return <Image src={image} height={250} alt={caption} />
        }
    }
</a>

<style>
    .gallery-item {
        border-radius: 4px;
        overflow: hidden;
    }
</style>