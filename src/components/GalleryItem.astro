---
import { Image } from "astro:assets";
const {image, caption, vertical = ""} = Astro.props;
---
<a class="gallery-item" href={image.src} title={caption}>
    {
        () => {
        if (vertical === "true") {
           return <Image src={image} width={250} alt={caption} />
        } else {
           return <Image src={image} height={250} alt={caption} />
        }
    }
</a>

<style>
    .gallery-item {
        border-radius: 4px;
        overflow: hidden;
    }
</style>